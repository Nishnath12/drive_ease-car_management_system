import React from'react';import{LayoutDashboard,MapPin,CarFront,CalendarDays,Wrench,ClipboardCheck,Package,Settings2,Users,BarChart3,ChevronLeft,ChevronRight,LogOut}from'lucide-react';import logo from'../assets/drivee.jpg';import Analytics from'./Analytics';import'../styles/Sidebar.css';
const common=[{id:'dashboard',label:'Overview',icon:LayoutDashboard},{id:'featured',label:'Vehicles',icon:CarFront},{id:'locations',label:'Showrooms',icon:MapPin}],customer=[{id:'garage',label:'My Garage',icon:CarFront},{id:'testdrive',label:'Test drives',icon:CalendarDays},{id:'bookings',label:'My bookings',icon:ClipboardCheck},{id:'services',label:'Service',icon:Wrench}],employee=[{id:'analytics',label:'Analytics',icon:BarChart3},{id:'approve-bookings',label:'Booking queue',icon:ClipboardCheck},{id:'parts',label:'Parts inventory',icon:Package}],supervisor=[{id:'analytics',label:'Analytics',icon:BarChart3},{id:'approve-bookings',label:'Booking queue',icon:ClipboardCheck},{id:'parts',label:'Parts inventory',icon:Package},{id:'car-management',label:'Vehicle management',icon:CarFront},{id:'manage-locations',label:'Manage showrooms',icon:Settings2},{id:'staff',label:'Staff management',icon:Users}];
const Sidebar=({activeSection,setActiveSection,user,handleLogout,sidebarExpanded=true,toggleSidebar})=>{const role=String(user?.role||'').toLowerCase();const items=[...common,...(role==='customer'?customer:role==='supervisor'?supervisor:employee)];const initials=(user?.name||'User').split(' ').map(p=>p[0]).join('').slice(0,2).toUpperCase();return <><aside className={`sidebar ${sidebarExpanded?'expanded':'collapsed'}`}><div className="sidebar-brand"><img src={logo} alt="DriveEase" className="sidebar-logo"/>{sidebarExpanded&&<div className="brand-copy"><strong>DriveEase</strong><span>Mobility platform</span></div>}<button className="sidebar-toggle" onClick={toggleSidebar} aria-label={sidebarExpanded?'Collapse sidebar':'Expand sidebar'}>{sidebarExpanded?<ChevronLeft size={17}/>:<ChevronRight size={17}/>}</button></div><div className="sidebar-profile"><div className="avatar">{initials}</div>{sidebarExpanded&&<div className="profile-copy"><strong>{user?.name||'User'}</strong><span>{role||'customer'}</span></div>}</div><nav className="sidebar-nav" aria-label="Main navigation"><span className="nav-caption">Workspace</span>{items.map(({id,label,icon:Icon})=><button key={id} className={`menu-item ${activeSection===id?'active':''}`} onClick={()=>setActiveSection(id)} title={!sidebarExpanded?label:undefined}><Icon size={19} strokeWidth={1.9}/>{sidebarExpanded&&<span className="menu-label">{label}</span>}</button>)}</nav><div className="sidebar-footer"><button className="logout-btn" onClick={handleLogout} title={!sidebarExpanded?'Sign out':undefined}><LogOut size={18}/>{sidebarExpanded&&<span>Sign out</span>}</button>{sidebarExpanded&&<small>DriveEase • Secure workspace</small>}</div></aside>{activeSection==='analytics'&&role!=='customer'&&<div className="analytics-overlay"><div className="analytics-overlay-inner"><button className="analytics-close" onClick={()=>setActiveSection('dashboard')}>×</button><Analytics/></div></div>}</>};export default Sidebar;
