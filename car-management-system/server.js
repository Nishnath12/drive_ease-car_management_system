const express=require("express");const dotenv=require("dotenv");const cors=require("cors");const helmet=require("helmet");const morgan=require("morgan");dotenv.config();const app=express();const PORT=Number(process.env.PORT)||5000;const NODE_ENV=process.env.NODE_ENV||"development";app.set("trust proxy",1);app.disable("x-powered-by");app.use(helmet());const configuredOrigins=process.env.FRONTEND_URL?process.env.FRONTEND_URL.split(",").map(v=>v.trim()).filter(Boolean):null;app.use(cors({origin:configuredOrigins?.length?configuredOrigins:true,methods:["GET","POST","PUT","PATCH","DELETE","OPTIONS"],allowedHeaders:["Content-Type","Authorization"],credentials:false}));app.use(express.json({limit:"1mb"}));app.use(express.urlencoded({extended:true,limit:"1mb"}));if(NODE_ENV!=="test")app.use(morgan(NODE_ENV==="development"?"dev":"combined"));
const authRoutes=require("./routes/auth"),carRoutes=require("./routes/cars"),locationRoutes=require("./routes/locations"),reservationRoutes=require("./routes/reservations"),approveBookingsRoutes=require("./routes/approveBookingsRoutes"),serviceRoutes=require("./routes/service"),sparePartsRoutes=require("./routes/spareParts"),sparePartBookingRoutes=require("./routes/sparePartBooking"),testDriveRoutes=require("./routes/testDriveRoutes"),staffRoutes=require("./routes/staff"),analyticsRoutes=require("./routes/analytics");
app.get("/health",(req,res)=>res.status(200).json({status:"ok",service:"car-management-system"}));app.get("/api/health",(req,res)=>res.status(200).json({status:"ok",service:"car-management-system"}));app.use("/api/auth",authRoutes);app.use("/api/cars",carRoutes);app.use("/api/locations",locationRoutes);app.use("/api/reservations",reservationRoutes);app.use("/api/approve-bookings",approveBookingsRoutes);app.use("/api/services",serviceRoutes);app.use("/api/spare-parts",sparePartsRoutes);app.use("/api/spare-bookings",sparePartBookingRoutes);app.use("/api/testdrives",testDriveRoutes);app.use("/api/staff",staffRoutes);app.use("/api/analytics",analyticsRoutes);app.get("/",(req,res)=>res.json({message:"Welcome to the Car Management System API"}));app.use((req,res)=>res.status(404).json({message:"Route not found"}));app.use((err,req,res,next)=>{console.error(err.stack||err);res.status(500).json({message:"Something went wrong!"});});if(require.main===module){app.listen(PORT,"0.0.0.0",()=>console.log(`Server running in ${NODE_ENV} mode on port ${PORT}`));}module.exports=app;
